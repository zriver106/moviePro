# moviePro 项目规则

进入本仓库先读 `CLAUDE.md` 与 `工作流.md`；它们的项目制作规则同样适用于 Codex。

## 已有视频纠错（2026-09-15 Andy明确要求）

已有视频有问题，不重新生成，利用编辑去改问题。后期问题本机编辑；画面问题以原视频调用 fal.ai Seedance 2.5 `task: "editing"`。禁止用默认 `reference`、换 seed 重抽、单镜重渲或整段重生代替编辑，编辑失败也不自动回落到重新生成。仅 Andy 后续明确授权重新生成时例外。

保留原片，编辑结果另存，复验后替换对应片段。接口参数、输入限制、付费授权及验收流程见 `docs/验收与纠错.md`。本规则更新不代表已有代码已实现自动拦截。

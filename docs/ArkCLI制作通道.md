# Ark CLI 制作通道（2026-09-25 Andy明确要求）

后续 Seedance、Seedream 及已有 480p 视频提升到 1080p，统一使用已安装并登录的火山方舟 Ark CLI（arkcli）。本规则覆盖历史文档中的 fal.ai 默认通道和 fal.ai 专用编辑路由；历史参数仅作旧任务记录，不能照搬到 Ark。

| 任务 | 指定通道 | 执行要求 |
|---|---|---|
| Seedream 出图、改图 | Ark CLI | 查询当前可调用资源及精确版本参数后执行 |
| Seedance 新视频制作、已有视频编辑 | Ark CLI | 区分首次生成与编辑，不用参考生成冒充已有视频编辑 |
| 已有 480p 视频提质到 1080p | Ark CLI | 以原片进行真正的超分/增强；保留剧情、人物、动作、镜头、时序与音轨，保留原片并另存结果 |

## 保留的制作约束

- 已有视频只编辑，不重新生成。不能用重抽 seed、参考视频重生成或重新生成一条 1080p 视频代替提质；只有 Andy 后续明确授权重新生成才可例外。
- 提质输出需检查实际分辨率、画幅、时长、帧率、音轨同步与跨帧一致性；仅把尺寸放大不等于质量提升。验收后才替换交付片段。
- 评级门禁、先交完整首版、现有预算与付费授权范围继续有效。确定通道不等于授权立刻生成或批量消费。
- 调用失败或功能缺失时保留原片、任务 ID 和具体错误，不静默换供应商、模型、账号、收费路径或重新提交生成。

## 接入状态与边界

已安装 Ark CLI 1.0.36 及配套 Skills，并完成过 SSO 登录验证。实际执行时按相关 arkcli Skill 重新核对有效认证、资源及模型参数。

2026-09-25 检查本地 `arkcli +gen --help`：有图片/视频生成、参考输入及输出分辨率参数；没有据此确认独立的已有视频超分或正式编辑能力。`--resolution 1080p` 只描述输出规格，不能当作超分证明。执行已有视频提质或编辑前，须通过官方契约和实际资源能力确认对应入口；未确认时保持待接入，不能编造命令、照搬 fal 的 `task: editing` 或使用生成兜底。

本次是制作通道规则更新，没有改动 `scripts/provider.py`、完成后端迁移或提交付费任务。业务脚本继续通过 provider 统一入口；后续执行上述任务前，必须先核对并完成 Ark CLI 适配，不能直接运行仍默认 fal 的旧脚本。

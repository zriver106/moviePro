# CLAUDE.md

## Ark CLI 统一通道（2026-09-25，优先于下文历史供应商说明）

Seedance、Seedream，以及已有 480p 视频提质到 1080p，统一走 Ark CLI。已有视频只编辑、不重新生成；提质必须基于原片，不能用重新生成 1080p 代替。旧 fal.ai 默认与专用路由不再作为后续执行依据。能力核验、提质验收及尚未完成的代码接入见 [docs/ArkCLI制作通道.md](docs/ArkCLI制作通道.md)。

短剧自动化制作。给在这个仓库里工作的 Claude Code 看。

**先读 `工作流.md`** —— 一集从头到尾怎么走。这一篇只写跨环节的铁律。

## 模型分流（2026-09-25 Andy明确要求）

打斗、一镜到底或前期投流精彩片段，任一超过15秒必须 Seedance-2.5；不超过15秒的打斗、风景、飞行、大动作及高画质镜头优先 Seedance-2.0；简单画面、对话和剧情推进优先 Seedance-2.0-mini。mini 并发占满时，尚未提交的同类任务可转 Seedance-2.0-fast；两者各并发3，不能重复提交同一任务。出图固定 Seedream-5.0-pro。未覆盖场景按质量、连续性、成本和吞吐自行决策。完整优先级、15秒边界及共享并发规则见 [Ark CLI 制作通道](docs/ArkCLI制作通道.md)。本条覆盖旧模型默认，不代表执行脚本已实现自动调度。 **已有 Seedance-2.5 或 Seedance-2.0 视频要修改指定几秒时，使用 Seedance-2.5 的视频编辑能力，经 Ark CLI，以原片为输入；这项用途不受15秒新片选型条件限制。**

## 已有视频用编辑修复（2026-09-15 Andy明确要求）

2026-09-17 Andy纠正交付顺序：先完成整集首版并发给他看片，再谈修改。不得在首版前自行反复追加画面修复、卡住完整交付。总包V4第一集按98秒制作，保留真实问题记录；首版不等于最终验收。见 `工作流.md` 的“首版先交用户看”。

**已有视频有问题，不重新生成，利用编辑修改问题。** 禁止换 seed 重抽、单镜重渲或整段重生作为纠错或失败兜底。字幕、剪辑、独立音轨等后期问题先本机修；人物、动作、背景等画面问题经 Ark CLI 使用 Seedance-2.5 的正式视频编辑能力，以现有视频为输入并明确修改的起止时间；适用于 2.5 或 2.0 已生成的视频。具体 Ark 编辑入口须先核验，历史 fal 参数不直接套用。编辑失败继续诊断编辑路径，不自动改成 `reference` 或新生成；只有 Andy 后续明确授权重新生成才能例外。

保留原片，定位问题时间段和修改目标，编辑结果另存版本，验收后替换成片中的对应片段。具体端点、参数及限制以 `docs/验收与纠错.md` 为准；网络调用仍统一走 `scripts/provider.py`。本规则不等于已完成代码闸或已执行付费编辑。

## 出图前必须先过独立评级（2026-09-14 Andy纠正）

先完成当前剧本与文字分镜，送“小说评级”任务真实评审；分别达到标准后才允许生成角色、道具、场景、白模和关键帧。低于标准先按具体问题返修、复评，不能自行调分、使用旧小说评级或用 `--force` 绕过。默认门槛S，适用维度不得平均抵消；具体预算写进剧目评级规则。《一寸活路》当前每集60秒，第一集22镜、第二集17镜、第三集18镜。时长求和不等于动作实际可演完。

独立评级入口：本地任务“小说评级”，threadId `01a08ea0-8c4d-74e0-ae4b-95fa647cfb4d`。流程与证据格式见 `docs/文本评级门禁.md`；代码闸为 `scripts/rating_gate.py`。首次审阅确认实际阅读范围，后续按差异及前后文复核；已经通过的成熟内容不无故重开。剧本或分镜字节变化使该版评级立即过期。历史84镜快照不能作为新稿评级或生成依据。

**Andy最新澄清：不是所有文字都要评级。** 评级只针对剧本、文字分镜，以及直接改变剧情、对白、人物动机、因果、动作顺序或剧情所需连续性的内容。资产ID、文件名、路径、索引、调用表、状态记录、格式整理及不改变剧情的制作参数不送“小说评级”，由制作环节自查。单纯把已通过的剧情准确落实到资源或修正图片偏差，不另做文字评级；若制作中确实要改剧情，先同步回剧本/分镜并独立复评。按实际内容判断，不能因为文件是JSON、提示词或含文字就一律送评。

跟 `~/.claude/CLAUDE.md` 里的加密货币量化项目无关；那份里只有
「直接决策、不问、中文简洁汇报」的沟通原则在这里继续生效。

---

## 项目定位

```
短剧制作      剧本 → 分镜 → 资产 → 关键帧 → 视频 → 剪辑成片
工作流引擎    串起每一步，并在每一步之间设门禁
资源调度      mini/fast 各并发 3，按模型分池；整批总上限 10
```

全链路走云端 API。

## 技术栈

| 环节 | 用什么 | 密钥 |
|---|---|---|
| 剧本审查 | MiniMax-M3（推理模型，输出带 `<think>` 要剥） | `key/sk-api` |
| 出图 / 合成 | Seedream-5.0-pro，经 Ark CLI，具体编辑能力先核对 | Ark CLI 认证 |
| 出片 | Seedance-2.5 / 2.0 / 2.0-mini / 2.0-fast，按模型分流规则经 Ark CLI 执行 | Ark CLI 认证 |
| ASR 回读 | whisper（`transcribe`） | 见供应商 |
| 后期 | 本机 ffmpeg（字幕要 `ffmpeg-full`） | — |
| 发片 | Telegram | `key/tg-token` |

**密钥只由 `scripts/keys.py` 读**，别在脚本里各写各的路径。别打印、别写日志。

## 供应商是可替换的

**所有出图/出片/ASR 的网络调用只从 `scripts/provider.py` 走**，
业务脚本里不许出现任何服务商的域名或端点路径。

```
MOVIEPRO_BACKEND=fal        中介，当前默认
MOVIEPRO_BACKEND=official   官方直连（正式项目用，接的时候只改 provider.py）
```

换供应商时业务脚本一行都不用动，但**下面这些跟端点绑死的事必须重新验**
（在 `provider.capabilities()` 里声明，业务脚本查它做决策，别自己写死）：

```
data URI 支不支持     出图端点和视频端点可能不一样
时长 / 分辨率上限     2.0 是 15s/720p，2.5 是 30s/1080p
参考图张数上限        9 / 30
内容策略             2.5 拒收写实真人参考图和含人视频
参数名与枚举值        duration 是字符串还是整数、resolution 的写法
```

**上限一律在调用前检查并报错，不许静默截断** —— 提交 30 秒回来 15 秒
而不报错，是这类 bug 里最难查的一种。

---

## 五条铁律

### 一、否定即点名

跟模型说「不要 X」，它听见的是「X」。

```
写 no cut        硬切率 43% → 86%
写 no speech     无人镜里冒出人声
写 no zoom       变焦 57% → 32%      ← 反向，见下
```

**判据：那个行为是不是模型本来就想做的。** 想做的（切、门），点名等于批准；
不太想做的（变焦、空镜里放人），点名等于禁止。抽象排除词基本无效，
要点名到具体物件（「现代物件」压不住，「键盘/显示器/电线」才压得住）。

**默认规则：一个否定词都不写，只描述要什么。** 检查时逐个搜
`no`、`not`、`never`、`without`、`only`、`avoid`、`don't` ——
`only` 也算（「只有 X」等于「除了 X 都没有」，是排除式表达）。

词表在 `scripts/negwords.py`，**只此一份**。分两份的后果：跑主链路的那份
漏了 `only`，37 处 `only` 一路放行到成片。

**分清通道**：有独立 negative prompt 输入的模型，负向写在那里是有效的；
但 4-step 蒸馏模型 cfg 钉在 1.0，负向在数学上被完全抵消。

### 二、图和字冲突时，图赢

参考图是控制面，prompt 是修饰。**想换造型必须换图，不能只换字。**

```
prompt 写「月白色圆领常服袍」，参考图是现代 T 恤   →  出来是 T 恤
prompt 写「皮肤保持参考图颜色」，图本身泡在红光里   →  改不掉（+101 → +100）
```

推论：

```
每个非基底造型都要有自己的参考图
渲出来不对，先怀疑措辞不对（模型不认的词要换成可指认的描述）
写得越具体，矛盾越致命 —— 具体化只在不和图打架时才是增益
```

**但参考图控制不了构图。** 把角色图裁成头肩再喂进去，输出画面几乎没变化
（46%/40%/30% 三档控制变量测过）。构图由模型的人像先验加画布长宽比决定。

**同一条规律在两个环节答案相反**：出静态图时参考图跟文字抢构图 → 控制不了构图；
`reference-to-video` 里参考图只承担身份 → 构图完全听 prompt。
**规律要带着适用边界一起记。**

分工划死：

```
构图、景别、角度、画面里的人长什么样   →  关键帧管（出图时没有参考构图，文字是唯一来源，写死是对的）
中段别引入新东西：人数、地点、身份     →  视频 prompt 管（已有关键帧定构图，再写景别只会矛盾）
```

### 三、语域铁律：给人看的文本不能直接喂模型

分镜和资产定义是写给人看的，混着声音事件、内心活动、创作意图、跨空间的地理信息。
**模型不区分「这句要执行」和「这句是背景说明」，照单全收。**

```
中文 = 台词             画面描述一律英文；中文只出现在台词字段里
                        `story_function` 这类意图字段完全不进 prompt
动作必须是可见的         声音事件整条删（归音频轨）；
                        内心活动改成可见的身体信号（「他在等」→「视线停在一处不动」）
锚点按可见性过滤         室内镜的锚点只能是这个房间里看得见的东西
单人镜的主语和宾语       都只能是这个人自己的身体
```

第四条要写成语义界定，**不是列黑名单**：先禁「对方/众人」，模型改用「前方脸上」——
「脸」不是人称代词，搜关键词搜不出来，但这句话预设了一张不属于说话人自己的脸。

配套两条工程规矩：

```
清理残渣的规则必须排在制造残渣的规则之后   顺序反了会跑出「改了 0 条」而残留几百条
改写规则本身会变时，识别不能依赖上一版输出  要按模式识别，不能精确匹配旧模板
```

### 四、静默兜底是最贵的 bug

**主路径失败后进来的本就是不确定输入，却用了最不设防的取值方式；
全都不报错，全都产出「看起来合理」的错数据。**

| 兜底 | 后果 |
|---|---|
| `all([])` 返回 True | 「所有条件都满足」，实际一个都没检查 |
| 说话人匹配不上就继承上一个 | 台词张冠李戴 |
| 找不到造型图就回落到通用图 | 古装戏里穿现代 T 恤 |
| 字体找不到就回落拉丁字形 | 整集字幕豆腐块，**退出码还是 0** |
| `min(上限, 请求值)` | 提交 30 秒回来 15 秒，不报错 |
| zsh 里 `for f in $LIST` | 不分词，整串当一个文件名，静默跳过 |
| 检查脚本的正则漏一类字符 | 报「表里 0 个」，看起来像结论 |

**写兜底时的判断标准：这个回落值如果错了，下游能不能发现？**
发现不了就不许静默回落 —— 要么报错，要么进 `blocked` 列表。

拦法优先用**已有的门禁通道**，不要每处新开一个报错出口。
一个大家都认的闸门，比五个各自为政的 `raise` 有用。

**门禁要一次报全部再退出**，不要撞到第一个就退 ——
那会逼人「改一个→重跑→又发现一个」跑很多遍。

### 五、派生字段必须跟着源字段一起更新

源字段和派生字段各自看都合理，**只有并排看才发现对不上**。

```
声景从动作关键词推出来 → 动作重写了、声景没重算 → 声景在描述镜头里不存在的事
剧本改了 → 分镜/资产/成片还是旧剧本的产物 → 每一层单独看都正常
中文分镜手写 → JSON 一改就过期 → 两份对不上
```

对策：**派生的东西必须是生成的，不是手抄的**；源头要有指纹闸
（`script_lock.py` / `base_lock.py`），改了下游立刻报错。

改了源字段就问一句：**谁是从它推出来的。**

---

## 协作

**别覆盖别人正在用的产物。** 两条机制缺一不可：

```
在用清单进文件不进参数     靠人记得传参数是靠不住的
要改产物就换版本号         重出先落新文件名，比过再换；覆盖等于在对方脚下换地板
```

跨会话共用一个仓库时，**「产出在不在」不能只信上游汇报** —— 汇报可能是真的，
文件是后来被别人删的，两件事都成立。动 `scripts/` 里的文件之前先看
`git log` 最近提交是不是别人的。

**语法检查用 `compile()` 不用 `ast.parse`** —— 后者只解析不做流分析，
不在循环里的 `continue` 它看不出来，报「语法 ok」，运行才炸。

## Git

日常改动推 `dev`。**每次改完代码手动 commit，注释写清楚改了什么、为什么改**，
禁止 `auto: code update` 这类空洞注释。

```bash
git add -A && git commit -m "改动说明（中文，写清楚改了什么、为什么）

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

## 索引

| 文档 | 管什么 |
|---|---|
| `工作流.md` | **一集从头到尾**，每步花不花钱、过哪几道门禁 |
| `docs/立项规则.md` | 开工前要定清楚哪些事 |
| `docs/asset-management.md` | 资产六条铁律 |
| `docs/打斗分镜制作.md` | 打戏：牌面、密度、运镜、特效、形态切换 |
| `docs/验收与纠错.md` | 成片怎么验、发现问题选哪条修法 |
| `docs/白模故事板.md` | 白模先行 |
| `docs/配音情绪.md` | 配音 |

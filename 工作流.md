# moviePro 工作流

**一句话**：

```
立项 → 剧本 → 剧本医生 → ⓪定稿 → 分镜 → 资产 → 关键帧 → 出片 → 验收纠错 → 成片
                          ↑                                        ↓
                    冻文字 + 冻样子                          小问题当场改，
                    改了下游全废                              别重渲整条
```

**两处跟直觉不一样，是这条链路上最贵的两课：**

- **⓪ 定稿是闸，不是仪式。** 分两半 —— 剧本定稿冻文字，定妆定稿冻常驻角色和
  高频道具/场景的基础样式。两边都记**内容指纹**：改了，下游立刻报错。
  没有这道闸，剧本还在动分镜和资产就开做，改一句台词上游全废。
- **验收之后不是重渲，是纠错。** 发现问题按代价从小到大挑修法，
  重渲是最后手段（详见 `docs/验收与纠错.md`）。

四层，各管各的，边界是硬的：

```
规格层   .claude/skills/    产出文字规格。**一行 API 都不调**
蒸馏层   cangjie-skill      长文档 → 可自动触发的 skill
执行层   scripts/           **只有这一层花钱**。网络调用只从 provider.py 走
剧目层   projects/<剧名>/    每部戏自己的剧本、分镜、资产、成片
```

**判据一句话：产出文字规格 → skill；产出图和视频 → `scripts/`。**

---

## 一集从头到尾

```bash
P=漫剧战斗样片            # 剧目名
V=.venv/bin/python        # 系统 Python 是 PEP 668，装不了包
```

### ⓪ 定稿 —— 两半，都是下游的前置

定稿分两半：**剧本定稿**冻文字，**定妆定稿**冻样子。两个都过了才能开工。

```bash
# 一、剧本定稿（冻文字）
$V scripts/script_doctor.py --project $P --ep 1 --out projects/$P/out/doctor
$V scripts/script_lock.py   --project $P --ep 1 --lock

# 二、定妆定稿（冻样子）—— 常驻角色 / 高频道具 / 高频场景各一张基础样式图
$V scripts/base_lock.py --project $P --scan            # 扫剧本按频次排清单
#   → 人逐条填 prompt（可指认的部件，不写抽象词）
$V scripts/base_lock.py --project $P --gen --jobs 10   # 一个一张，单张不要多面
#   → 人看图
$V scripts/base_lock.py --project $P --lock            # 锁指纹
```

**定妆定稿只回答一个问题：这个人／这件道具／这个地方长什么样。**
多面、表情、动作、状态、派生形态**全部留给资产阶段，并且必须从这张基础图
edit 派生**，不许另起炉灶重画。

基础样式图三条硬要求：

```
中性光       环境光归场景管。定妆照带环境光会被模型当成角色固有色 ——
             场景光一变就冲突，成片里每处肤色都不一样（实测脸部 R−B 到 +150）
单张正面     多面是资产阶段的事
可指认的部件  「一支精致的细银簪」三次画成银球/宽刃剑/巨型簪；
             写成「锥形细杆＋扁平五瓣梅花＋花心珍珠＋针尖」一次就对
```

**为什么必须锁**：没有被锁住的母板，同一个东西每次生成都会漂。
派生形态各画各的会变成三个不同的人；同一件家具在两个资产里会是两种木色，
前后两镜切过去就像两个地方。

#### 剧本定稿的四种状态

**拿到剧本别急着做事，先定稿。** 剧本还在改就做分镜和资产，改一句台词上游全废；
更隐蔽的是**改了剧本下游没重跑** —— 分镜、关键帧、成片都还是旧剧本的产物，
每一层单独看都正常，只有并排看才发现对不上。

所以定稿记的是**内容指纹**，不是一句「定了」：

```bash
$V scripts/script_lock.py --project $P --ep 1              # 看状态
$V scripts/script_lock.py --project $P --ep 1 --lock       # 定稿（要求医生判 PASS）
$V scripts/script_lock.py --project $P --ep 1 --relock --note "改了什么"
```

```
locked     指纹对得上，下游可以开工
stale      **剧本在定稿之后改过** —— 下游全部作废，要重新定稿
unlocked   还没定稿
```

`compile_prompts.py` 开头就查这道闸，没定稿直接拦下 ——
分镜、资产、关键帧、出片全都在它后面，一道闸挡住整条链路。

医生没判 PASS 不给定稿。确实要带着已知问题开工，用 `--force` 并且
**必须在 `--note` 里写清楚为什么**。


### ① 文本层 —— 不花钱

| 做什么 | 用什么 |
|---|---|
| 立项 | `docs/立项规则.md` 逐条填成 `projects/<剧名>/立项.md` |
| 小说 → 剧本 | `/short-drama-novel-analyze` → `/short-drama-develop` → `/short-drama-write` |
| 剧本体检 | `$V scripts/script_doctor.py --project $P --ep 1 --out projects/$P/out/doctor` |
| **剧本定稿** | `$V scripts/script_lock.py --project $P --ep 1 --lock` ← **前置闸，见下** |
| 剧本 → 分镜 | `/short-drama-storyboard`（有 Coverage Audition 和连续性检查） |
| **分镜 → 中文稿** | `$V scripts/board_readable.py --project $P --ep 1 --html` ← **给人看的那份** |
| 打戏分镜 | 另看 `docs/打斗分镜制作.md` —— 十节实测规律 |

#### 分镜有两份，别混

```
分镜/EPxxx.json        **给 agent 的**：英文渲染文案、时间切片、prompt、门禁字段
分镜/EPxxx_中文.md     **给人看的**：这一秒发生什么、镜头怎么走、谁说什么、怎么接
分镜/EPxxx_中文.html   同上，浏览器里看，手机也能看
```

**中文那份是生成的，不许手改。** 之前是手写的，于是 JSON 一改它就过期，
两份对不上 —— 跟「声景没跟着 `action_start` 重算」是同一个病。
JSON 是唯一权威，中文稿是它的投影，改完分镜重跑一次就同步。

### ② 编译 —— 不花钱，但这是所有门禁的入口

```bash
$V scripts/compile_prompts.py --project $P --ep 1
```

一次跑三道门禁，**任何一条不过都不该往下走**：

```
否定词      写「no cut」硬切率 43%→86%。词表只有一份（negwords.py），only 也算
情绪词      「calm」「fierce」这类抽象词模型演不出来，要写可见的身体信号
身体动词    每个时间切片至少一个动词落在人身上 —— 写状态会得到站着不动的人
```

### ③ 资产 —— 花钱

```bash
$V scripts/gen_asset.py       --project $P --jobs 10   # 并发上限 10
$V scripts/measure_cells.py   --project $P             # bbox 实测，别用理论等分
$V scripts/vfx_alpha.py       --project $P             # 特效黑底 → RGBA 透明
$V scripts/check_assets_bg.py --project $P             # 背景门禁
$V scripts/check_skin.py      --project $P             # 肤色一致性
$V scripts/asset_gate.py      --project $P             # **只有 approved 放行**
```

规矩在 `docs/asset-management.md`。最要紧的几条：一资产一 sheet 多状态一次生成、
写可指认的部件不写抽象词、家具从场景里拆出来、改资产一次只改一件事、
**重出先落新文件名比过再换**。

### ④ 关键帧 —— 花钱

```bash
$V scripts/gen_kf.py          --project $P --ep 1 --jobs 10
$V scripts/check_keyframes.py --project $P --ep 1
```

**人看图对清单再开渲。** 出总览图是为了并排看 —— 「近景一半渲成暗调夜景」
这种问题逐张看永远发现不了。

### ⑤ 出片 —— 花钱，**要人点头才能跑**

```bash
$V scripts/render_seedance.py --project $P --ep 1 --model 2.5 --jobs 10
```

| | Seedance 2.0 | Seedance 2.5 |
|---|---|---|
| 参考图 / 跨模态总数 | 9 / 12 | **30 / 50** |
| 时长 | 4–15s | **4–30s** |
| 分辨率 | 480p / 720p | 480p / 720p / **1080p** |
| 写实真人参考图 | 收 | **拒**（内容策略，文档没写） |
| 动漫角色参考图 | 收 | 收 |

**第一版一律 480p** —— 大概率是废片，定稿了再上分辨率。
长片一致性走 `ref2v`（全套资产当 `@ImageN`），别走 `i2v`（只锚首帧，切一次镜就换脸）。

### ⑥ 验收与纠错 —— **详见 `docs/验收与纠错.md`**

```bash
$V scripts/check_cuts.py     成片.mp4 --threshold 0.4   # 打戏 0.4，静态 0.1
$V scripts/check_dialogue.py --project $P --ep 1        # ASR 回读比对分镜原文
ffprobe -v error -show_entries format=duration -of csv=p=0 成片.mp4
ffmpeg -i 成片.mp4 -vf "fps=1,scale=300:-1,tile=6x5" -frames:v 1 grid.png   # 并排看
```

机器先筛（时长/台词/切镜/静止/烧字幕），人只看机器判不出的三类：
**内容对不对、一致性、观感**。

**发现问题按代价从小到大选修法，重渲是最后手段：**

```
要求其余像素不变        → ① 本机 ffmpeg 修
全局性（色调/背景/氛围） → ② $V scripts/fix_video.py $F --fix "..." --seconds 6
换 seed 会变            → ③ 单镜重渲
换 seed 不变（结构失败） → ④ 回上游改分镜或资产
```

`fix_video.py` 把成片当 `@Video1` 喂回 ref2v。**它不是像素级的改，是照着原片
重做** —— 结构时序保住，细节会重掷。含人的视频**只有 2.0 收**（2.5 拒），
所以修片上限 15 秒 720p，30 秒要分段。

**代理指标只在「被测的东西是画面里唯一在剧烈变化的东西」时成立。**
特效打戏里剑气扫过镜头会被当成切镜 —— 指标失效时换指标，不是放宽阈值。

### ⑦ 后期 —— 不花钱

```bash
$V scripts/cut_points.py      --project $P --ep 1   # 剪辑入出点
$V scripts/assemble.py        --project $P --ep 1   # 拼片 + 字幕
$V scripts/assemble_takes.py  --project $P --ep 1   # 长镜版，字幕轴取自 ASR 时间戳
$V scripts/finale_overlay.py  --project $P --ep 1   # 片尾定格 + 按锚点贴图
```

---

## 脚本清单（29 个，全部接 `--project`）

| 组 | 脚本 | 花钱 |
|---|---|---|
| 文本 | `script_doctor` `script_lock` `adapt_script` `make_board` `m3` | MiniMax |
| 定妆 | `base_lock` | 出图/出片 |
| 编译 | `compile_prompts` `negwords` `keys` | 否 |
| 给人看 | `board_readable` | 否 |
| 资产 | `gen_asset` `gen_whitemodel` | 出图/出片 |
| 资产加工 | `measure_cells` `vfx_alpha` `whiten_bg` | 否 |
| 关键帧 | `gen_kf` `gen_kf_chain` | 出图/出片 |
| 出片 | `render_seedance` | 出图/出片 |
| 门禁验收 | `asset_gate` `check_assets_bg` `check_skin` `check_keyframes` `check_cuts` `check_dialogue` | 否 |
| 纠错 | `fix_video` | 出片（2.0） |
| 后期 | `cut_points` `assemble` `assemble_takes` `finale_overlay` | 否 |

---

## 剧目

| 剧目 | 状态 |
|---|---|
| `搞笑办公室连续剧1` | EP001 已交付（60s，写实真人，Seedance 2.0） |
| `漫剧战斗样片` | 30 秒打戏样片迭代中（日式动漫，Seedance 2.5） |
| `躺平神医` | 立项 |

---

## 方法论文档

| 文档 | 管什么 |
|---|---|
| `docs/立项规则.md` | 一部戏开工前要定清楚哪些事 |
| `docs/asset-management.md` | 资产六条铁律 |
| `docs/验收与纠错.md` | 成片怎么验、发现问题选哪条修法 |
| `docs/打斗分镜制作.md` | 打戏专用：运镜、密度、特效、形态切换 |
| `docs/白模故事板.md` | 白模先行 |
| `docs/配音情绪.md` | 配音 |
| `docs/外部工作流研究.md` | 外部工具调研 |
| `CLAUDE.md` | 全局铁律：否定即点名、图字冲突图赢、静默兜底 |

